-- ============================================================
-- Google 連携の失効を記録する
--
-- 行があるかだけを見て「連携済み」と表示していたため、
-- トークンが失効しても気付けず、同期されていないまま過ごす恐れがあった。
-- 失効を検知した時点で印を立て、画面で押す前に分かるようにする。
-- ============================================================

alter table public.google_connections
  add column needs_reconnect boolean not null default false;
