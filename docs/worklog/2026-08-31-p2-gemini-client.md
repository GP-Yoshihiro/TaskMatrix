# 作業ログ: Gemini クライアントの実機検証（2026-08-31）

## 何を

実装した `createGeminiTaskExtractor()` で実際に Gemini API を呼び、
抽出とフォールバックが機能することを確認した。

## R-21 に基づく送信内容

検証用に作成した架空の定例メモ（約 90 文字）のみを送信した。
プロジェクトの実データ・ファイル名・アカウント情報は送信していない。

## 結果

| 検証項目 | 結果 |
|---|---|
| モデルのフォールバック | ✅ `gemini-3.7-flash` が 5xx → `gemini-3.5-flash` で成功 |
| 応答時間 | 21.4 秒（入力 276 / 出力 391 トークン） |
| 曖昧な期限「来週まで」「適宜」 | ✅ `due_date` は空文字。不透明点に理由を記載 |
| 確定日付「2026-09-10」 | ✅ `due_date` に正しく格納 |
| 担当者の抽出 | ✅ 「田中」を抽出 |
| 改善提案 | ✅ 具体的な日付合意を促す内容が日本語で生成された |
| キー未設定時 | ✅ `AI_NOT_CONFIGURED` を返す |

## わかったこと

- `gemini-3.7-flash` の混雑は一時的ではなく継続している。
  フォールバックを入れておいて正解だった。
- 応答に 20 秒超かかるため、Server Action に `maxDuration` が必須。
- ビルド成果物の確認: `GEMINI_API_KEY` の値は `.next/server/**` には
  インライン展開されるが、**クライアントに配信される `.next/static` には含まれない**。
  Next.js の正常な挙動。ただしビルド成果物は秘密を含むため配布しないこと。
  `.next` は `.gitignore` 済み。
