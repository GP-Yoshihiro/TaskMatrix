# 作業ログ: 第2フェーズ Task 5（タスク管理画面）の検証（2026-08-31）

## 実機で確認したこと

| 確認項目 | 結果 |
|---|---|
| タスク一覧の表示（優先度の高い順） | ✅ 高 → 中 → 低 の順で並ぶ |
| リスト / カンバンの切替 | ✅ `aria-pressed` も連動 |
| カンバンでのステータス移動 | ✅ 未着手3 → 未着手2 / 進行中1 |
| 手動でのタスク追加 | ✅ AI 抽出バッジが付かない |
| タスクの編集（既存値が入る） | ✅ 名前・担当・期限・ステータスが復元される |
| タスクの削除（ダイアログ経由） | ✅ キャンセルで残り、確定で消える |
| AI の指摘の折りたたみ表示 | ✅ 不透明点と改善提案がある場合のみ表示 |

## 5 形式すべてでの抽出（完了条件 2）

`extraction_runs` の記録より、すべて `succeeded`。

| ファイル | 形式 | 抽出件数 | モデル |
|---|---|---|---|
| 要件メモ.md | markdown | 4 | gemini-3.5-flash |
| 議事録PDF.pdf | pdf | 3 | gemini-3.5-flash |
| 会議メモ.docx | docx | 3 | gemini-3.5-flash |
| 進捗表.xlsx | xlsx | 1 | gemini-3.5-flash |
| キックオフ.pptx | pptx | 1 | gemini-3.5-flash |

いずれも既定の `gemini-3.7-flash` が混雑しており、フォールバックが作動している。

## 非破壊の確認（完了条件 4）

同じファイルから再抽出しても、既存の 3 件のタスクは件数も内容も変わらなかった。
カンバンで「進行中」に変更した状態もそのまま保持されていた。

## RLS の確認（完了条件 10）

別ユーザーになりすました状態で、タスクは 0 件しか見えず、
UPDATE と DELETE も 0 行にしか影響しなかった（実データ 3 件は無傷）。

## 期限の扱いについての観察

AI は「9月10日」のように**年が欠けた日付**も曖昧と判断し、
`due_date` を空にしたうえで不透明点に「実施年（YYYY）が不明です」と記載した。
設計の意図どおりに機能している。
