# TaskMatrix 第4フェーズ（RAG チャット）実装計画

> **エージェント実行者へ:** `superpowers:executing-plans` を用いてタスク単位で実装すること。

**ゴール:** プロジェクト内のファイルを横断検索し、根拠付きで質問に答えられるようにする。

**設計書:** `docs/specs/2026-08-31-p4-rag-chat-design.md`

---

## グローバル制約

- UI 文言・コメント・コミットメッセージはすべて**日本語**（R-02）
- 機能ごとに新規ブランチ。`main` へ直接コミットしない（R-03）
- **`lint` / `typecheck` / `test` / `build` がすべてグリーンでなければコミットしない**（R-05 / R-06）
- テストを先に書く（R-12）
- **依存パッケージを追加しない**（R-19）
- **自動テストで実 API を呼ばない**
- 埋め込みモデル: `gemini-embedding-2`、次元数 **768**
- **一括埋め込みは `{ parts: [{ text }] }` の配列で渡す。**
  文字列の配列は黙って 1 件に統合されるため使わない
- **返却件数と入力件数の一致を必ず検証する**
- 近傍検索の SQL 関数は **`security invoker`**。`security definer` にしない
- RLS ポリシーでは外側の列をテーブル名で修飾する

---

## Task 1: pgvector とデータモデル、チャンク分割

**ブランチ:** `feature/rag-schema`

**Files:**
- Create: `supabase/migrations/0006_rag.sql`
- Create: `lib/domain/chunk.ts`, `lib/domain/__tests__/chunk.test.ts`

**Interfaces:**
- Produces:
  - `const CHUNK_SIZE = 800`
  - `const CHUNK_OVERLAP = 100`
  - `const MAX_CHUNKS_PER_PROJECT = 300`
  - `const EMBEDDING_DIMENSIONS = 768`
  - `function splitIntoChunks(text: string): string[]`
  - `function validateChunkCount(count: number): Result<number>`
  - DB: `file_chunks` / `chat_sessions` / `chat_messages` / `match_file_chunks`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main && git pull --ff-only && git checkout -b feature/rag-schema
```

- [ ] **Step 2: 失敗するテストを書く**

`lib/domain/__tests__/chunk.test.ts` で次を検証する。

- 短い文章は 1 チャンクになる
- `CHUNK_SIZE` を超えたら複数に分かれる
- 各チャンクが `CHUNK_SIZE` 以下（重なりを含めても超えない）
- **段落の切れ目（空行）を優先して分割する**
- 段落が無ければ改行、それも無ければ句点（。）で切る
- どれも無い長文は強制的に切る（無限ループにしない）
- 隣り合うチャンクが `CHUNK_OVERLAP` 程度重なる
- 空文字・空白のみは空配列を返す
- 空白のみのチャンクは捨てる
- 日本語が壊れない（文字単位で切る）
- `validateChunkCount` が上限超過を `TOO_MANY_CHUNKS` で拒否する
- 上限ちょうどは受け入れる

- [ ] **Step 3: テストを実行し、失敗を確認する**

- [ ] **Step 4: `lib/domain/chunk.ts` を実装する**

分割の方針:

1. まず段落（`\n\n` 以上）で区切る
2. 段落が `CHUNK_SIZE` に収まるなら、収まる限り連結する
3. 単独で超える段落は、改行 → 句点 → 強制切断の順で細かくする
4. 連結の際、直前のチャンク末尾から `CHUNK_OVERLAP` 文字を引き継ぐ

**無限ループを避けるため、どの分岐でも必ず 1 文字以上進めること。**

- [ ] **Step 5: マイグレーションを作成する**

`supabase/migrations/0006_rag.sql`:

```sql
-- pgvector を有効化する。次元数 768 は固定長のため、
-- 変更するにはテーブルを作り直す必要がある
create extension if not exists vector with schema extensions;

create table public.file_chunks (
  id           uuid primary key default gen_random_uuid(),
  project_id   uuid not null references public.projects(id) on delete cascade,
  file_id      uuid not null references public.files(id) on delete cascade,
  file_version integer not null,
  chunk_index  integer not null,
  content      text not null,
  embedding    extensions.vector(768),
  created_at   timestamptz not null default now(),
  unique (file_id, file_version, chunk_index)
);

create index file_chunks_project_idx on public.file_chunks (project_id);
create index file_chunks_file_idx    on public.file_chunks (file_id);
create index file_chunks_embedding_idx
  on public.file_chunks using hnsw (embedding extensions.vector_cosine_ops);

create table public.chat_sessions (
  id         uuid primary key default gen_random_uuid(),
  project_id uuid not null references public.projects(id) on delete cascade,
  title      text not null default '',
  created_by uuid not null references public.profiles(id) on delete cascade,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index chat_sessions_project_idx
  on public.chat_sessions (project_id, updated_at desc);

create table public.chat_messages (
  id         uuid primary key default gen_random_uuid(),
  session_id uuid not null references public.chat_sessions(id) on delete cascade,
  role       text not null check (role in ('user', 'assistant')),
  content    text not null,
  sources    jsonb not null default '[]'::jsonb,
  created_at timestamptz not null default now()
);

create index chat_messages_session_idx
  on public.chat_messages (session_id, created_at);

-- ============================================================
-- 行レベルセキュリティ
-- ============================================================

alter table public.file_chunks   enable row level security;
alter table public.chat_sessions enable row level security;
alter table public.chat_messages enable row level security;

create policy file_chunks_all_own on public.file_chunks
  for all to authenticated
  using (
    exists (
      select 1 from public.projects p
      where p.id = file_chunks.project_id and p.owner_id = (select auth.uid())
    )
  )
  with check (
    exists (
      select 1 from public.projects p
      where p.id = file_chunks.project_id and p.owner_id = (select auth.uid())
    )
  );

create policy chat_sessions_all_own on public.chat_sessions
  for all to authenticated
  using (
    exists (
      select 1 from public.projects p
      where p.id = chat_sessions.project_id and p.owner_id = (select auth.uid())
    )
  )
  with check (
    exists (
      select 1 from public.projects p
      where p.id = chat_sessions.project_id and p.owner_id = (select auth.uid())
    )
  );

create policy chat_messages_all_own on public.chat_messages
  for all to authenticated
  using (
    exists (
      select 1 from public.chat_sessions s
      join public.projects p on p.id = s.project_id
      where s.id = chat_messages.session_id and p.owner_id = (select auth.uid())
    )
  )
  with check (
    exists (
      select 1 from public.chat_sessions s
      join public.projects p on p.id = s.project_id
      where s.id = chat_messages.session_id and p.owner_id = (select auth.uid())
    )
  );

-- ============================================================
-- 近傍検索
-- security invoker にすること。definer にすると RLS を迂回して
-- 他人のチャンクを返してしまう
-- ============================================================

create function public.match_file_chunks(
  target_project_id uuid,
  query_embedding extensions.vector(768),
  match_count integer
)
returns table (
  id uuid,
  file_id uuid,
  chunk_index integer,
  content text,
  similarity double precision
)
language sql
stable
security invoker
set search_path = ''
as $$
  select
    c.id,
    c.file_id,
    c.chunk_index,
    c.content,
    1 - (c.embedding operator(extensions.<=>) query_embedding) as similarity
  from public.file_chunks c
  where c.project_id = target_project_id
    and c.embedding is not null
  order by c.embedding operator(extensions.<=>) query_embedding
  limit greatest(1, least(match_count, 20));
$$;

revoke execute on function public.match_file_chunks(uuid, extensions.vector, integer) from public;
grant execute on function public.match_file_chunks(uuid, extensions.vector, integer) to authenticated;
```

- [ ] **Step 6: マイグレーションを適用する**

Supabase MCP の `apply_migration` を `name = 0006_rag` として適用する。

- [ ] **Step 7: 適用結果を確認する**

- `list_extensions` で `vector` の `installed_version` が入っていること
- `list_tables` で 3 テーブルが `rls_enabled: true` であること
- `get_advisors`（security）で新たな警告が出ていないこと

- [ ] **Step 8: RLS と関数の安全性を SQL で確認する**

別ユーザーになりすまし、次を確認する。

1. `file_chunks` / `chat_sessions` / `chat_messages` が 0 件
2. INSERT / UPDATE / DELETE が効かない
3. **`match_file_chunks` を呼んでも他人のチャンクが返らない**
   （`security invoker` が効いていることの確認。ここが最重要）

- [ ] **Step 9: 検証セットを実行し、グリーンならコミットする**

---

## Task 2: 埋め込みの作成

**ブランチ:** `feature/rag-indexing`

**Files:**
- Create: `lib/gemini/embeddings.ts`, `lib/gemini/__tests__/embeddings.test.ts`
- Create: `lib/repositories/file-chunks.ts`
- Create: `lib/usecases/build-index.ts`, `lib/usecases/__tests__/build-index.test.ts`
- Create: `lib/actions/rag.ts`
- Create: `components/app/rag-index-panel.tsx`
- Create: `app/(app)/projects/[projectId]/chat/page.tsx`

**Interfaces:**
- Produces:
  - `interface Embedder { embed(texts: string[]): Promise<Result<number[][]>> }`
  - `function createGeminiEmbedder(): Embedder`
  - `interface FileChunkRepository { deleteByFile; insertMany; updateEmbeddings; countByProject }`
  - `function buildIndexForProject(deps, input): Promise<Result<{ files: number; chunks: number }>>`
  - Server Action: `buildIndexAction(formData)`

- [ ] **Step 1: ブランチを作成する**

- [ ] **Step 2: 埋め込み器の失敗するテストを書く**

`lib/gemini/__tests__/embeddings.test.ts`。**実 API は呼ばない。**
SDK をモックし、次を検証する。

- **`contents` に `{ parts: [{ text }] }` の配列を渡している**
  （文字列の配列を渡していないこと。渡すと 1 件に統合される）
- `outputDimensionality` に 768 を指定している
- 返却件数が入力件数と一致しない場合 `EMBEDDING_COUNT_MISMATCH` を返す
- 次元数が 768 でない場合も拒否する
- 入力が空配列なら API を呼ばずに空配列を返す
- キー未設定なら `AI_NOT_CONFIGURED`

- [ ] **Step 3〜4: 実装する**

`lib/gemini/embeddings.ts`:

```typescript
const EMBEDDING_MODEL = 'gemini-embedding-2'

export function createGeminiEmbedder(): Embedder {
  return {
    async embed(texts) {
      if (texts.length === 0) return ok([])

      const apiKey = process.env.GEMINI_API_KEY
      if (!apiKey) return err('AI_NOT_CONFIGURED', 'AI 機能が設定されていません。')

      const ai = new GoogleGenAI({ apiKey })

      try {
        const response = await withTimeout(
          ai.models.embedContent({
            model: process.env.GEMINI_EMBEDDING_MODEL || EMBEDDING_MODEL,
            // 文字列の配列を渡すと黙って 1 件に統合される。必ず parts 形式にする
            contents: texts.map((text) => ({ parts: [{ text }] })),
            config: { outputDimensionality: EMBEDDING_DIMENSIONS },
          }),
          REQUEST_TIMEOUT_MS,
        )

        const vectors = (response.embeddings ?? []).map((e) => e.values ?? [])

        // 件数がずれると、別のチャンクの意味が入った検索結果になる
        if (vectors.length !== texts.length) {
          return err('EMBEDDING_COUNT_MISMATCH', '検索用データの作成に失敗しました。もう一度お試しください。')
        }
        if (vectors.some((v) => v.length !== EMBEDDING_DIMENSIONS)) {
          return err('EMBEDDING_COUNT_MISMATCH', '検索用データの作成に失敗しました。もう一度お試しください。')
        }

        return ok(vectors)
      } catch {
        return err('AI_REQUEST_FAILED', 'AI への問い合わせに失敗しました。時間をおいてお試しください。')
      }
    },
  }
}
```

- [ ] **Step 5: 作成ユースケースのテストを書く**

`buildIndexForProject` で次を検証する（埋め込み器とテキスト抽出器はモック）。

- markdown は DB の本文、binary は Storage から取り出す
- 同じファイルの古い版のチャンクを先に削除する
- チャンク数が上限を超えたら `TOO_MANY_CHUNKS` で拒否し、API を呼ばない
- 埋め込みの件数不一致をそのまま利用者向けエラーとして返す
- 本文が空のファイルは飛ばす
- 対象ファイルが 0 件なら `NO_INDEXED_CONTENT` を返す
- 埋め込みは一定件数ずつに分けて呼ぶ（既定 32 件）

- [ ] **Step 6〜7: 実装し、テストを通す**

- [ ] **Step 8: 画面を作る**

`app/(app)/projects/[projectId]/chat/page.tsx` と
`components/app/rag-index-panel.tsx`。

- 「検索用データを作成」ボタン
- **送信内容の確認ダイアログ（R-21）**：
  「ファイルの本文が Gemini API に送信されます」と明示する
- 作成中の待機表示
- 完了時に「N 個のファイルから M 件のデータを作成しました。」

プロジェクト画面とタスク画面から `/chat` へのリンクを足す。

- [ ] **Step 9: 検証セットと実機確認**

1. 検索用データを作成でき、件数が表示される
2. `file_chunks` に `embedding` が入っている（SQL で確認）
3. ファイルを編集して再作成すると、古い版のチャンクが消えている

- [ ] **Step 10: グリーンならコミットする**

---

## Task 3: 質問と回答、会話履歴

**ブランチ:** `feature/rag-chat`

**Files:**
- Create: `lib/domain/rag.ts`, `lib/domain/__tests__/rag.test.ts`
- Create: `lib/gemini/answer-question.ts`
- Create: `lib/repositories/chat.ts`
- Create: `lib/usecases/answer-question.ts`, `lib/usecases/__tests__/answer-question.test.ts`
- Create: `components/app/rag-chat.tsx`
- Modify: `lib/actions/rag.ts`

**Interfaces:**
- Produces:
  - `type ChatSource = { fileId: string; fileName: string; chunkIndex: number; excerpt: string }`
  - `function buildExcerpt(content: string, maxLength?: number): string`
  - `function trimHistory<T>(messages: T[], maxTurns?: number): T[]`
  - `function buildAnswerPrompt(input): string`
  - `function answerQuestion(deps, input): Promise<Result<{ answer: string; sources: ChatSource[] }>>`
  - Server Actions: `askAction(formData)`, `createSessionAction(formData)`

- [ ] **Step 1: ブランチを作成する**

- [ ] **Step 2: ドメインの失敗するテストを書く**

`lib/domain/rag.ts` について:

- `buildExcerpt` が長い本文を指定長に切り詰め、末尾に `…` を付ける
- 短い本文はそのまま返す（`…` を付けない）
- 改行を空白に置き換えて 1 行にする
- **日本語が壊れない**
- `trimHistory` が直近 N 往復だけ残す（既定 6 往復 = 12 件）
- 履歴が少ないときはそのまま返す
- `buildAnswerPrompt` が
  - 質問文を含む
  - 抜粋にファイル名を添える
  - **「資料からは分かりません」と答えるよう指示している**
  - 推測で答えないよう指示している

- [ ] **Step 3〜4: 実装し、テストを通す**

- [ ] **Step 5: 回答ユースケースのテストを書く**

`answerQuestion` で次を検証する（埋め込み器と Gemini はモック）。

- 質問を埋め込みに変換して近傍検索を呼ぶ
- 近傍が 0 件なら `NO_INDEXED_CONTENT` を返し、Gemini を呼ばない
- 上位 8 件までに絞る
- 根拠（ファイル名・抜粋）を組み立てて返す
- 直近の会話を切り詰めて渡す
- 質問と回答の両方を保存する
- 埋め込みが失敗したらそのエラーを返し、Gemini を呼ばない

- [ ] **Step 6〜7: 実装し、テストを通す**

- [ ] **Step 8: チャット画面を作る**

`components/app/rag-chat.tsx`:

- 会話の表示（利用者の質問と回答を区別）
- 回答の下に**根拠**を折りたたみで表示（ファイル名＋抜粋）
- 質問の入力欄と送信ボタン
- 送信中の待機表示（`callAction` で包む）
- 検索用データが無いときの案内
- 送信内容の説明を常時表示（R-21）

- [ ] **Step 9: 検証セットと実機確認**

1. 質問して根拠付きの回答が返る
2. **資料に無いことを聞くと「資料からは分かりません」と答える**
3. 会話が保存され、再読み込み後も表示される
4. 検索用データが無い状態で質問すると案内が出る
5. RLS を SQL で確認（別ユーザーから 0 件、`match_file_chunks` でも漏れない）

- [ ] **Step 10: グリーンならコミットする**

---

## 自己レビュー結果

**1. 設計書の網羅性**

| 設計書の項目 | 対応タスク |
|---|---|
| §3 一括埋め込みの落とし穴 | Task 2 Step 2・4（テストで固定） |
| §5 データモデル・RLS・検索関数 | Task 1 |
| §6 チャンク分割 | Task 1 Step 2・4 |
| §7.1 埋め込みの作成 | Task 2 |
| §7.2 質問への回答 | Task 3 |
| §8 エラー処理 | Task 1〜3 |
| §9 テストと検証 | 全タスク |
| §11 依存追加なし | 全タスク |
| §12 完了条件 1〜11 | Task 1 Step 8 / Task 2 Step 9 / Task 3 Step 9 |

**2. プレースホルダ**

Task 1 Step 4 と Task 3 Step 8 は方針を文章で示しているが、
分割の優先順位・上限・表示項目・文言を具体的に指定している。

**3. 特に注意する点**

- **`security invoker`**: `match_file_chunks` を `definer` にすると
  RLS を迂回して他人のチャンクが返る。Task 1 Step 8 で必ず確認する
- **件数の一致検証**: 一括埋め込みは黙って件数が減る。
  検証を外すと、あるチャンクの本文に別のチャンクの意味が結び付き、
  検索結果が静かに壊れる
- **古い版のチャンク削除**: 消し忘れると古い記述を根拠に回答してしまう
