# TaskMatrix 第3フェーズ（スケジュール自動算出）実装計画

> **エージェント実行者へ:** `superpowers:executing-plans` を用いてタスク単位で実装すること。
> 手順はチェックボックス (`- [ ]`) 形式で追跡する。

**ゴール:** 未完了タスクから AI が実行日時を算出し、重複を明示した仮案として提示し、
ユーザーが編集・確定してカレンダーに反映できる状態にする。.ics で書き出せる。

**アーキテクチャ:** 読み取りは Server Components、書き込みは Server Actions（P1・P2 を継承）。
`.ics` の書き出しのみ Route Handler を使う。
AI の出力する日時は信用せず、稼働条件の範囲内かをサーバー側で検証する。
重複の検出は AI に任せず、`lib/domain/schedule.ts` の純粋関数で決定的に行う。

**技術スタック:** 追加依存なし。日付は標準の `Date` と `Intl`、`.ics` とカレンダー UI は自前実装。

**設計書:** `docs/specs/2026-08-31-p3-schedule-design.md`

---

## グローバル制約

- 応答・コメント・UI 文言・コミットメッセージはすべて**日本語**（R-02）
- 機能ごとに**新規ブランチ**。`main` へ直接コミットしない（R-03）
- 各タスクの最後に `npm run lint` → `npm run typecheck` → `npm test` → `npm run build`。
  **すべてグリーンでのみコミット**（R-05 / R-06）
- テストを先に書く（R-12）
- 各ブランチ完了時に Claude が PR を作成し、承認を得てマージ（R-04）
- Supabase プロジェクト ref: `patasstmipeqaaovfihv`
- **依存パッケージを追加しない**（R-19。設計書 §11）
- **自動テストで実 API を呼ばない**。Gemini クライアントは必ずモックする
- タスクの重さは 5 段階: `very_heavy` / `heavy` / `normal` / `light` / `very_light`
- 稼働条件の既定値: 月〜金 / 09:00〜18:00 / 1 日 360 分 / `Asia/Tokyo`
- 重複の定義: `aStart < bEnd && bStart < aEnd`（**隣接は重複としない**）
- RLS ポリシーでは外側の列を**テーブル名で修飾**する（P1 の不具合の再発防止）
- 承認要求時・完了時に `say -v Kyoko "..."` で音声通知（R-08）

---

## ファイル構成

| ファイル | 責務 |
|---|---|
| `lib/domain/schedule.ts` | 重さの定義と表示名、稼働日・時間帯の判定、範囲補正、**重複検出** |
| `lib/domain/ics.ts` | `.ics` 文字列の組み立て（エスケープ・折り返し・UTC 変換） |
| `lib/domain/calendar.ts` | 月間グリッドの組み立て |
| `lib/repositories/schedules.ts` | `schedules` のデータアクセス |
| `lib/repositories/work-settings.ts` | `work_settings` のデータアクセス |
| `lib/gemini/plan-schedule.ts` | 算出プロンプトと構造化スキーマ |
| `lib/usecases/plan-schedule.ts` | 算出ユースケース（依存を引数で受け取る） |
| `lib/actions/schedules.ts` | 算出・確定の Server Actions |
| `lib/actions/work-settings.ts` | 稼働条件の Server Action |
| `components/app/schedule-planner.tsx` | 算出実行と仮案の管理 |
| `components/app/schedule-draft-item.tsx` | 仮案 1 件の表示・日時編集・重複の明示 |
| `components/app/overlap-warning-dialog.tsx` | 確定時の重複警告 |
| `components/app/calendar-month.tsx` | 月間ビュー |
| `components/app/work-settings-form.tsx` | 稼働条件の設定 |
| `app/(app)/projects/[projectId]/schedule/page.tsx` | スケジュール画面 |
| `app/api/projects/[projectId]/schedule.ics/route.ts` | `.ics` 書き出し |

---

## Task 1: スケジュールのデータモデルとドメインロジック

**ブランチ:** `feature/schedule-schema`

**Files:**
- Create: `supabase/migrations/0005_schedules.sql`
- Create: `lib/domain/schedule.ts`, `lib/domain/__tests__/schedule.test.ts`

**Interfaces:**
- Consumes: `Result` `ok` `err`
- Produces:
  - `type TaskWeight = 'very_heavy' | 'heavy' | 'normal' | 'light' | 'very_light'`
  - `const TASK_WEIGHTS: readonly TaskWeight[]`
  - `const WEIGHT_LABEL: Record<TaskWeight, string>`
  - `const WEIGHT_OVERLAP_HINT: Record<TaskWeight, string>`
  - `function isTaskWeight(value: string): value is TaskWeight`
  - `type TimeRange = { startsAt: string; endsAt: string }`
  - `function overlaps(a: TimeRange, b: TimeRange): boolean`
  - `function findOverlaps<T extends TimeRange & { id: string }>(target: T, others: T[]): T[]`
  - `type WorkSettings = { workDays: number[]; workStart: string; workEnd: string; dailyCapacityMinutes: number; timezone: string }`
  - `const DEFAULT_WORK_SETTINGS: WorkSettings`
  - `function isWorkDay(isoDateTime: string, settings: WorkSettings): boolean`
  - `function isWithinWorkHours(range: TimeRange, settings: WorkSettings): boolean`
  - `function validateWorkSettings(input: {...}): Result<WorkSettings>`
  - DB テーブル `schedules` / `work_settings`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/schedule-schema
```

- [ ] **Step 2: 失敗するテストを書く**

`lib/domain/__tests__/schedule.test.ts` を作成する。

```typescript
import { describe, expect, it } from 'vitest'
import {
  DEFAULT_WORK_SETTINGS,
  TASK_WEIGHTS,
  WEIGHT_LABEL,
  WEIGHT_OVERLAP_HINT,
  findOverlaps,
  isTaskWeight,
  isWithinWorkHours,
  isWorkDay,
  overlaps,
  validateWorkSettings,
} from '@/lib/domain/schedule'

describe('タスクの重さ', () => {
  it('5 段階を重い順に定義する', () => {
    expect([...TASK_WEIGHTS]).toEqual([
      'very_heavy',
      'heavy',
      'normal',
      'light',
      'very_light',
    ])
  })

  it('すべての段階に日本語表示名がある', () => {
    for (const weight of TASK_WEIGHTS) {
      expect(WEIGHT_LABEL[weight]).toBeTruthy()
    }
    expect(WEIGHT_LABEL.very_heavy).toBe('非常に重い')
    expect(WEIGHT_LABEL.very_light).toBe('非常に軽い')
  })

  it('すべての段階に重複の目安がある', () => {
    for (const weight of TASK_WEIGHTS) {
      expect(WEIGHT_OVERLAP_HINT[weight]).toBeTruthy()
    }
  })

  it('型ガードが正しく判定する', () => {
    expect(isTaskWeight('very_heavy')).toBe(true)
    expect(isTaskWeight('heavy')).toBe(true)
    expect(isTaskWeight('extreme')).toBe(false)
    expect(isTaskWeight('')).toBe(false)
  })
})

describe('overlaps', () => {
  const base = { startsAt: '2026-09-01T09:00:00+09:00', endsAt: '2026-09-01T11:00:00+09:00' }

  it('完全に重なる期間を重複とする', () => {
    expect(overlaps(base, base)).toBe(true)
  })

  it('一部が重なる期間を重複とする', () => {
    expect(
      overlaps(base, {
        startsAt: '2026-09-01T10:00:00+09:00',
        endsAt: '2026-09-01T12:00:00+09:00',
      }),
    ).toBe(true)
  })

  it('内側に含まれる期間を重複とする', () => {
    expect(
      overlaps(base, {
        startsAt: '2026-09-01T09:30:00+09:00',
        endsAt: '2026-09-01T10:00:00+09:00',
      }),
    ).toBe(true)
  })

  it('隣接（終了時刻＝開始時刻）は重複としない', () => {
    expect(
      overlaps(base, {
        startsAt: '2026-09-01T11:00:00+09:00',
        endsAt: '2026-09-01T12:00:00+09:00',
      }),
    ).toBe(false)
  })

  it('離れた期間は重複としない', () => {
    expect(
      overlaps(base, {
        startsAt: '2026-09-02T09:00:00+09:00',
        endsAt: '2026-09-02T11:00:00+09:00',
      }),
    ).toBe(false)
  })

  it('順序を入れ替えても同じ結果になる', () => {
    const other = {
      startsAt: '2026-09-01T10:00:00+09:00',
      endsAt: '2026-09-01T12:00:00+09:00',
    }
    expect(overlaps(base, other)).toBe(overlaps(other, base))
  })

  it('タイムゾーン表記が異なっても同じ時刻なら重複と判定する', () => {
    expect(
      overlaps(base, {
        startsAt: '2026-09-01T01:00:00Z', // = 10:00 JST
        endsAt: '2026-09-01T02:00:00Z',
      }),
    ).toBe(true)
  })
})

describe('findOverlaps', () => {
  const a = { id: 'a', startsAt: '2026-09-01T09:00:00+09:00', endsAt: '2026-09-01T11:00:00+09:00' }
  const b = { id: 'b', startsAt: '2026-09-01T10:00:00+09:00', endsAt: '2026-09-01T12:00:00+09:00' }
  const c = { id: 'c', startsAt: '2026-09-02T09:00:00+09:00', endsAt: '2026-09-02T10:00:00+09:00' }

  it('重複している相手だけを返す', () => {
    expect(findOverlaps(a, [b, c]).map((x) => x.id)).toEqual(['b'])
  })

  it('自分自身は除外する', () => {
    expect(findOverlaps(a, [a, b]).map((x) => x.id)).toEqual(['b'])
  })

  it('重複がなければ空配列を返す', () => {
    expect(findOverlaps(c, [a, b])).toEqual([])
  })
})

describe('isWorkDay', () => {
  it('既定では月〜金を稼働日とする', () => {
    // 2026-09-01 は火曜日
    expect(isWorkDay('2026-09-01T09:00:00+09:00', DEFAULT_WORK_SETTINGS)).toBe(true)
  })

  it('既定では土日を稼働日としない', () => {
    // 2026-09-05 は土曜日、2026-09-06 は日曜日
    expect(isWorkDay('2026-09-05T09:00:00+09:00', DEFAULT_WORK_SETTINGS)).toBe(false)
    expect(isWorkDay('2026-09-06T09:00:00+09:00', DEFAULT_WORK_SETTINGS)).toBe(false)
  })

  it('稼働曜日を変更すると判定も変わる', () => {
    const weekend = { ...DEFAULT_WORK_SETTINGS, workDays: [0, 6] }
    expect(isWorkDay('2026-09-05T09:00:00+09:00', weekend)).toBe(true)
    expect(isWorkDay('2026-09-01T09:00:00+09:00', weekend)).toBe(false)
  })
})

describe('isWithinWorkHours', () => {
  it('稼働時間帯に収まっていれば true', () => {
    expect(
      isWithinWorkHours(
        { startsAt: '2026-09-01T09:00:00+09:00', endsAt: '2026-09-01T12:00:00+09:00' },
        DEFAULT_WORK_SETTINGS,
      ),
    ).toBe(true)
  })

  it('開始が稼働開始より early なら false', () => {
    expect(
      isWithinWorkHours(
        { startsAt: '2026-09-01T08:00:00+09:00', endsAt: '2026-09-01T10:00:00+09:00' },
        DEFAULT_WORK_SETTINGS,
      ),
    ).toBe(false)
  })

  it('終了が稼働終了を超えたら false', () => {
    expect(
      isWithinWorkHours(
        { startsAt: '2026-09-01T17:00:00+09:00', endsAt: '2026-09-01T19:00:00+09:00' },
        DEFAULT_WORK_SETTINGS,
      ),
    ).toBe(false)
  })

  it('境界ちょうどは範囲内とする', () => {
    expect(
      isWithinWorkHours(
        { startsAt: '2026-09-01T09:00:00+09:00', endsAt: '2026-09-01T18:00:00+09:00' },
        DEFAULT_WORK_SETTINGS,
      ),
    ).toBe(true)
  })

  it('日をまたぐ期間は false', () => {
    expect(
      isWithinWorkHours(
        { startsAt: '2026-09-01T17:00:00+09:00', endsAt: '2026-09-02T10:00:00+09:00' },
        DEFAULT_WORK_SETTINGS,
      ),
    ).toBe(false)
  })
})

describe('validateWorkSettings', () => {
  it('既定値を受け入れる', () => {
    const result = validateWorkSettings(DEFAULT_WORK_SETTINGS)
    expect(result.ok).toBe(true)
  })

  it('稼働曜日が空なら拒否する', () => {
    const result = validateWorkSettings({ ...DEFAULT_WORK_SETTINGS, workDays: [] })
    expect(result.ok).toBe(false)
    if (!result.ok) expect(result.error.code).toBe('VALIDATION_ERROR')
  })

  it('範囲外の曜日を拒否する', () => {
    expect(validateWorkSettings({ ...DEFAULT_WORK_SETTINGS, workDays: [7] }).ok).toBe(false)
    expect(validateWorkSettings({ ...DEFAULT_WORK_SETTINGS, workDays: [-1] }).ok).toBe(false)
  })

  it('開始が終了以降なら拒否する', () => {
    const result = validateWorkSettings({
      ...DEFAULT_WORK_SETTINGS,
      workStart: '18:00',
      workEnd: '09:00',
    })
    expect(result.ok).toBe(false)
  })

  it('1 日の上限が 0 以下なら拒否する', () => {
    expect(
      validateWorkSettings({ ...DEFAULT_WORK_SETTINGS, dailyCapacityMinutes: 0 }).ok,
    ).toBe(false)
  })

  it('1 日の上限が稼働時間を超えたら拒否する', () => {
    // 09:00-18:00 は 540 分
    expect(
      validateWorkSettings({ ...DEFAULT_WORK_SETTINGS, dailyCapacityMinutes: 541 }).ok,
    ).toBe(false)
  })

  it('曜日の重複を取り除いて昇順にする', () => {
    const result = validateWorkSettings({
      ...DEFAULT_WORK_SETTINGS,
      workDays: [3, 1, 3, 2],
    })
    expect(result.ok).toBe(true)
    if (result.ok) expect(result.data.workDays).toEqual([1, 2, 3])
  })
})
```

- [ ] **Step 3: テストを実行し、失敗することを確認する**

Run: `npm test`
Expected: FAIL（`@/lib/domain/schedule` を解決できない）

- [ ] **Step 4: 最小の実装を書く**

`lib/domain/schedule.ts` を作成する。

```typescript
import { type Result, err, ok } from './result'

/** タスクの重さ。重い順に並べる */
export const TASK_WEIGHTS = [
  'very_heavy',
  'heavy',
  'normal',
  'light',
  'very_light',
] as const

export type TaskWeight = (typeof TASK_WEIGHTS)[number]

export const WEIGHT_LABEL: Record<TaskWeight, string> = {
  very_heavy: '非常に重い',
  heavy: '重い',
  normal: '標準',
  light: '軽い',
  very_light: '非常に軽い',
}

/** 重複の可否についての目安。利用者への助言として表示する */
export const WEIGHT_OVERLAP_HINT: Record<TaskWeight, string> = {
  very_heavy: '他の予定と重ねてはいけません。',
  heavy: '他の予定と重ねるべきではありません。',
  normal: 'できれば他の予定と重ねないでください。',
  light: '他の予定と重なっても差し支えないことが多いです。',
  very_light: '他の予定と重なっても支障はありません。',
}

export function isTaskWeight(value: string): value is TaskWeight {
  return (TASK_WEIGHTS as readonly string[]).includes(value)
}

export type TimeRange = { startsAt: string; endsAt: string }

/**
 * 2 つの期間が重なるか。
 * 終了時刻と開始時刻が同一の「隣接」は重複としない。
 */
export function overlaps(a: TimeRange, b: TimeRange): boolean {
  const aStart = Date.parse(a.startsAt)
  const aEnd = Date.parse(a.endsAt)
  const bStart = Date.parse(b.startsAt)
  const bEnd = Date.parse(b.endsAt)

  if ([aStart, aEnd, bStart, bEnd].some(Number.isNaN)) return false

  return aStart < bEnd && bStart < aEnd
}

/** target と重複している相手だけを返す。自分自身は除外する */
export function findOverlaps<T extends TimeRange & { id: string }>(
  target: T,
  others: T[],
): T[] {
  return others.filter((other) => other.id !== target.id && overlaps(target, other))
}

export type WorkSettings = {
  /** 0=日曜 〜 6=土曜 */
  workDays: number[]
  /** HH:MM */
  workStart: string
  /** HH:MM */
  workEnd: string
  dailyCapacityMinutes: number
  timezone: string
}

export const DEFAULT_WORK_SETTINGS: WorkSettings = {
  workDays: [1, 2, 3, 4, 5],
  workStart: '09:00',
  workEnd: '18:00',
  dailyCapacityMinutes: 360,
  timezone: 'Asia/Tokyo',
}

/** その設定のタイムゾーンで見た「曜日」と「時刻」を取り出す */
function partsIn(isoDateTime: string, timezone: string) {
  const date = new Date(isoDateTime)
  if (Number.isNaN(date.getTime())) return null

  const formatter = new Intl.DateTimeFormat('en-US', {
    timeZone: timezone,
    weekday: 'short',
    hour: '2-digit',
    minute: '2-digit',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour12: false,
  })

  const parts = Object.fromEntries(
    formatter.formatToParts(date).map((part) => [part.type, part.value]),
  )

  const weekdayIndex = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'].indexOf(
    parts.weekday ?? '',
  )

  return {
    weekday: weekdayIndex,
    minutes: Number(parts.hour) * 60 + Number(parts.minute),
    day: `${parts.year}-${parts.month}-${parts.day}`,
  }
}

function toMinutes(hhmm: string): number {
  const [hour, minute] = hhmm.split(':').map(Number)
  return hour * 60 + minute
}

export function isWorkDay(isoDateTime: string, settings: WorkSettings): boolean {
  const parts = partsIn(isoDateTime, settings.timezone)
  if (!parts) return false
  return settings.workDays.includes(parts.weekday)
}

/** 稼働時間帯に収まっているか。日をまたぐ期間は収まっていないものとする */
export function isWithinWorkHours(range: TimeRange, settings: WorkSettings): boolean {
  const start = partsIn(range.startsAt, settings.timezone)
  const end = partsIn(range.endsAt, settings.timezone)
  if (!start || !end) return false
  if (start.day !== end.day) return false

  return (
    start.minutes >= toMinutes(settings.workStart) &&
    end.minutes <= toMinutes(settings.workEnd) &&
    start.minutes < end.minutes
  )
}

export function validateWorkSettings(input: WorkSettings): Result<WorkSettings> {
  const workDays = [...new Set(input.workDays)].sort((a, b) => a - b)

  if (workDays.length === 0) {
    return err('VALIDATION_ERROR', '稼働する曜日を 1 つ以上選んでください。')
  }

  if (workDays.some((day) => !Number.isInteger(day) || day < 0 || day > 6)) {
    return err('VALIDATION_ERROR', '稼働曜日の指定が正しくありません。')
  }

  if (!/^\d{2}:\d{2}$/.test(input.workStart) || !/^\d{2}:\d{2}$/.test(input.workEnd)) {
    return err('VALIDATION_ERROR', '稼働時間の形式が正しくありません。')
  }

  const startMinutes = toMinutes(input.workStart)
  const endMinutes = toMinutes(input.workEnd)

  if (startMinutes >= endMinutes) {
    return err('VALIDATION_ERROR', '稼働終了は稼働開始より後の時刻にしてください。')
  }

  if (input.dailyCapacityMinutes <= 0) {
    return err('VALIDATION_ERROR', '1 日の上限は 1 分以上にしてください。')
  }

  if (input.dailyCapacityMinutes > endMinutes - startMinutes) {
    return err('VALIDATION_ERROR', '1 日の上限が稼働時間を超えています。')
  }

  return ok({ ...input, workDays })
}
```

- [ ] **Step 5: テストを実行し、成功することを確認する**

Run: `npm test`
Expected: PASS（schedule 30 件を含む全件）

- [ ] **Step 6: マイグレーション SQL を作成する**

`supabase/migrations/0005_schedules.sql` を作成する。

```sql
-- ============================================================
-- TaskMatrix 第3フェーズ スケジュールと稼働条件
-- ============================================================

create table public.schedules (
  id         uuid primary key default gen_random_uuid(),
  project_id uuid not null references public.projects(id) on delete cascade,
  task_id    uuid not null references public.tasks(id) on delete cascade,
  starts_at  timestamptz not null,
  ends_at    timestamptz not null,
  reason     text not null default '',
  weight     text not null default 'normal'
               check (weight in ('very_heavy','heavy','normal','light','very_light')),
  created_by uuid not null references public.profiles(id) on delete cascade,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint schedules_range_valid check (ends_at > starts_at)
);

create index schedules_project_idx on public.schedules (project_id, starts_at);
create index schedules_task_idx    on public.schedules (task_id);

create table public.work_settings (
  user_id                uuid primary key references public.profiles(id) on delete cascade,
  work_days              smallint[] not null default '{1,2,3,4,5}',
  work_start             time not null default '09:00',
  work_end               time not null default '18:00',
  daily_capacity_minutes integer not null default 360,
  timezone               text not null default 'Asia/Tokyo',
  updated_at             timestamptz not null default now(),
  constraint work_settings_hours_valid check (work_end > work_start),
  constraint work_settings_capacity_positive check (daily_capacity_minutes > 0)
);

-- ============================================================
-- 行レベルセキュリティ
-- サブクエリで projects を結合するため、外側の列はテーブル名で修飾する
-- ============================================================

alter table public.schedules     enable row level security;
alter table public.work_settings enable row level security;

create policy schedules_all_own on public.schedules
  for all
  to authenticated
  using (
    exists (
      select 1 from public.projects p
      where p.id = schedules.project_id and p.owner_id = (select auth.uid())
    )
  )
  with check (
    exists (
      select 1 from public.projects p
      where p.id = schedules.project_id and p.owner_id = (select auth.uid())
    )
  );

create policy work_settings_all_own on public.work_settings
  for all
  to authenticated
  using (work_settings.user_id = (select auth.uid()))
  with check (work_settings.user_id = (select auth.uid()));
```

- [ ] **Step 7: マイグレーションを適用する**

Supabase MCP の `apply_migration` を `project_id = patasstmipeqaaovfihv`、
`name = 0005_schedules` として適用する。

- [ ] **Step 8: 適用結果と RLS を確認する**

`list_tables`（スキーマ `public`）で `schedules` と `work_settings` が
`rls_enabled: true` で存在することを確認する。
`get_advisors`（`type: security`）で新たな警告が出ていないことを確認する。

- [ ] **Step 9: RLS が実効であることを SQL で確認する**

別ユーザーになりすまし、`schedules` と `work_settings` が 0 件であること、
INSERT / UPDATE / DELETE が効かないことを確認する（P2 Task 1 Step 9 と同じ手順）。

- [ ] **Step 10: 検証セットを実行する**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

- [ ] **Step 11: コミットする**

```bash
git add -A
git commit -m "feat(schedule): スケジュールのデータモデルとドメインロジックを追加

- schedules / work_settings テーブルとRLSポリシーを定義
- タスクの重さを5段階で定義し日本語表示名と重複の目安を用意
- 重複検出を純粋関数として実装（隣接は重複としない）
- 稼働日・稼働時間帯の判定と稼働条件の検証を実装"
```

---

## Task 2: 稼働条件の設定

**ブランチ:** `feature/work-settings`

**Files:**
- Create: `lib/repositories/work-settings.ts`
- Create: `lib/actions/work-settings.ts`
- Create: `components/app/work-settings-form.tsx`
- Modify: `app/(app)/settings/page.tsx`

**Interfaces:**
- Consumes: `WorkSettings` `DEFAULT_WORK_SETTINGS` `validateWorkSettings`（Task 1）
- Produces:
  - `interface WorkSettingsRepository { find(userId): Promise<WorkSettings | null>; save(userId, settings): Promise<void> }`
  - Server Action: `updateWorkSettingsAction(formData): Promise<Result<null>>`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/work-settings
```

- [ ] **Step 2: リポジトリを実装する**

`lib/repositories/work-settings.ts`:

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { WorkSettings } from '@/lib/domain/schedule'

export interface WorkSettingsRepository {
  find(userId: string): Promise<WorkSettings | null>
  save(userId: string, settings: WorkSettings): Promise<void>
}

type Row = {
  work_days: number[]
  work_start: string
  work_end: string
  daily_capacity_minutes: number
  timezone: string
}

/** DB の time 型は 'HH:MM:SS' で返るため 'HH:MM' に切り詰める */
function toHhMm(value: string): string {
  return value.slice(0, 5)
}

export function createSupabaseWorkSettingsRepository(
  supabase: SupabaseClient,
): WorkSettingsRepository {
  return {
    async find(userId) {
      const { data, error } = await supabase
        .from('work_settings')
        .select('work_days, work_start, work_end, daily_capacity_minutes, timezone')
        .eq('user_id', userId)
        .maybeSingle()
      if (error) throw error
      if (!data) return null

      const row = data as Row
      return {
        workDays: row.work_days,
        workStart: toHhMm(row.work_start),
        workEnd: toHhMm(row.work_end),
        dailyCapacityMinutes: row.daily_capacity_minutes,
        timezone: row.timezone,
      }
    },

    async save(userId, settings) {
      const { error } = await supabase.from('work_settings').upsert(
        {
          user_id: userId,
          work_days: settings.workDays,
          work_start: settings.workStart,
          work_end: settings.workEnd,
          daily_capacity_minutes: settings.dailyCapacityMinutes,
          timezone: settings.timezone,
          updated_at: new Date().toISOString(),
        },
        { onConflict: 'user_id' },
      )
      if (error) throw error
    },
  }
}
```

- [ ] **Step 3: Server Action を実装する**

`lib/actions/work-settings.ts`:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { type Result, err, ok } from '@/lib/domain/result'
import { validateWorkSettings } from '@/lib/domain/schedule'
import { createSupabaseWorkSettingsRepository } from '@/lib/repositories/work-settings'
import { createServerSupabaseClient } from '@/lib/supabase/server'

export async function updateWorkSettingsAction(formData: FormData): Promise<Result<null>> {
  const workDays = formData
    .getAll('workDays')
    .map((value) => Number(value))
    .filter((value) => Number.isInteger(value))

  const validated = validateWorkSettings({
    workDays,
    workStart: String(formData.get('workStart') ?? ''),
    workEnd: String(formData.get('workEnd') ?? ''),
    dailyCapacityMinutes: Number(formData.get('dailyCapacityMinutes') ?? 0),
    timezone: String(formData.get('timezone') ?? 'Asia/Tokyo'),
  })
  if (!validated.ok) return validated

  const supabase = await createServerSupabaseClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return err('UNAUTHENTICATED', 'ログインが必要です。')

  try {
    await createSupabaseWorkSettingsRepository(supabase).save(user.id, validated.data)
  } catch {
    return err('UNKNOWN', '稼働条件を保存できませんでした。')
  }

  revalidatePath('/settings')
  return ok(null)
}
```

- [ ] **Step 4: 設定フォームを実装する**

`components/app/work-settings-form.tsx` を作成する。
曜日はチェックボックス 7 個（`name="workDays"` `value="0"`〜`"6"`）、
稼働開始・終了は `type="time"`、1 日の上限は `type="number"`（分）とする。
保存後は「稼働条件を保存しました。」を表示する。
検証エラーは `role="alert"` で日本語表示する。

`app/(app)/settings/page.tsx` に「稼働条件」セクションとして組み込む。
未設定のユーザーには `DEFAULT_WORK_SETTINGS` を初期値として表示する。

- [ ] **Step 5: 検証セットを実行する**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

- [ ] **Step 6: 実際に動作を確認する**

1. 設定画面に稼働条件が既定値（月〜金 / 09:00-18:00 / 360 分）で表示される
2. 曜日を変更して保存でき、再読み込み後も保持される
3. 稼働終了を稼働開始より前にすると日本語のエラーが出る
4. 1 日の上限を稼働時間より大きくすると日本語のエラーが出る
5. Supabase MCP の `execute_sql` で `work_settings` に行が入っていることを確認する

- [ ] **Step 7: コミットする**

```bash
git add -A
git commit -m "feat(schedule): 稼働条件の設定を実装

- work_settings のリポジトリと upsert を実装
- 稼働曜日・時間帯・1日の上限を設定画面から変更できるようにした
- 未設定のユーザーには既定値を表示する"
```

---

## Task 3: AI によるスケジュール算出と仮案プレビュー

**ブランチ:** `feature/schedule-planner`

**Files:**
- Create: `lib/gemini/plan-schedule.ts`, `lib/gemini/__tests__/plan-schedule.test.ts`
- Create: `lib/repositories/schedules.ts`
- Create: `lib/usecases/plan-schedule.ts`, `lib/usecases/__tests__/plan-schedule.test.ts`
- Create: `lib/actions/schedules.ts`
- Create: `components/app/schedule-planner.tsx`
- Create: `app/(app)/projects/[projectId]/schedule/page.tsx`

**Interfaces:**
- Consumes: Task 1 のドメイン、Task 2 のリポジトリ、`TaskRepository`（P2）
- Produces:
  - `const SCHEDULE_SCHEMA`（構造化出力のスキーマ）
  - `function buildSchedulePrompt(input): string`
  - `function parseScheduleResponse(outputText: string): Result<{ schedules: RawSchedule[]; overall_note: string }>`
  - `interface SchedulePlanner { plan(input): Promise<Result<PlannedSchedules>> }`
  - `function createGeminiSchedulePlanner(): SchedulePlanner`
  - `type ScheduleDraft = { key: string; taskId: string; taskTitle: string; startsAt: string; endsAt: string; reason: string; weight: TaskWeight; overlapAcceptable: boolean; outOfWorkHours: boolean }`
  - `function planScheduleForProject(deps, input): Promise<Result<{ drafts: ScheduleDraft[]; note: string }>>`
  - `interface ScheduleRepository { listByProject; createMany; update; remove }`
  - Server Action: `planScheduleAction(formData): Promise<Result<{ drafts; note }>>`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/schedule-planner
```

- [ ] **Step 2: 失敗するテストを書く（プロンプトとスキーマ）**

`lib/gemini/__tests__/plan-schedule.test.ts`。**実 API は呼ばない。**

```typescript
import { describe, expect, it } from 'vitest'
import {
  SCHEDULE_SCHEMA,
  buildSchedulePrompt,
  parseScheduleResponse,
} from '@/lib/gemini/plan-schedule'
import { DEFAULT_WORK_SETTINGS } from '@/lib/domain/schedule'

const promptInput = {
  today: '2026-08-31',
  settings: DEFAULT_WORK_SETTINGS,
  tasks: [
    { title: '見積もりを提出する', description: '', priority: 'high', dueDate: '2026-09-10' },
  ],
  confirmed: [
    { taskTitle: '定例会議', startsAt: '2026-09-01T10:00:00+09:00', endsAt: '2026-09-01T11:00:00+09:00' },
  ],
}

describe('buildSchedulePrompt', () => {
  it('今日の日付を含める', () => {
    expect(buildSchedulePrompt(promptInput)).toContain('2026-08-31')
  })

  it('タスク名と期限を含める', () => {
    const prompt = buildSchedulePrompt(promptInput)
    expect(prompt).toContain('見積もりを提出する')
    expect(prompt).toContain('2026-09-10')
  })

  it('稼働条件を含める', () => {
    const prompt = buildSchedulePrompt(promptInput)
    expect(prompt).toContain('09:00')
    expect(prompt).toContain('18:00')
  })

  it('確定済みの予定を含める', () => {
    expect(buildSchedulePrompt(promptInput)).toContain('定例会議')
  })

  it('重さを 5 段階で判定するよう指示する', () => {
    const prompt = buildSchedulePrompt(promptInput)
    expect(prompt).toContain('very_heavy')
    expect(prompt).toContain('very_light')
  })

  it('日本語で理由を書くよう指示する', () => {
    expect(buildSchedulePrompt(promptInput)).toContain('日本語')
  })

  it('確定済みの予定が無いときも壊れない', () => {
    expect(() => buildSchedulePrompt({ ...promptInput, confirmed: [] })).not.toThrow()
  })
})

describe('SCHEDULE_SCHEMA', () => {
  it('schedules と overall_note を必須にする', () => {
    expect([...SCHEDULE_SCHEMA.required]).toEqual(['schedules', 'overall_note'])
  })

  it('重さを 5 段階に限定する', () => {
    const weight = SCHEDULE_SCHEMA.properties.schedules.items.properties.weight
    expect([...weight.enum]).toEqual([
      'very_heavy',
      'heavy',
      'normal',
      'light',
      'very_light',
    ])
  })
})

describe('parseScheduleResponse', () => {
  const valid = JSON.stringify({
    schedules: [
      {
        task_title: '見積もりを提出する',
        starts_at: '2026-09-01T09:00:00+09:00',
        ends_at: '2026-09-01T11:00:00+09:00',
        reason: '期限まで余裕があるうちに着手するため。',
        weight: 'heavy',
        overlap_acceptable: false,
      },
    ],
    overall_note: '優先度の高いものから順に配置しました。',
  })

  it('正しい JSON を解釈する', () => {
    const result = parseScheduleResponse(valid)
    expect(result.ok).toBe(true)
    if (result.ok) expect(result.data.schedules).toHaveLength(1)
  })

  it('壊れた JSON を拒否する', () => {
    const result = parseScheduleResponse('JSONではない')
    expect(result.ok).toBe(false)
    if (!result.ok) expect(result.error.code).toBe('AI_RESPONSE_INVALID')
  })

  it('想定外の重さを拒否する', () => {
    const broken = JSON.parse(valid)
    broken.schedules[0].weight = 'extreme'
    expect(parseScheduleResponse(JSON.stringify(broken)).ok).toBe(false)
  })

  it('コードフェンス付きでも解釈する', () => {
    expect(parseScheduleResponse('```json\n' + valid + '\n```').ok).toBe(true)
  })

  it('予定が 0 件でも成功として扱う', () => {
    const empty = JSON.stringify({ schedules: [], overall_note: 'なし' })
    expect(parseScheduleResponse(empty).ok).toBe(true)
  })
})
```

- [ ] **Step 3: テストを実行し、失敗することを確認する**

Run: `npm test`
Expected: FAIL（`@/lib/gemini/plan-schedule` を解決できない）

- [ ] **Step 4: プロンプトとスキーマを実装する**

`lib/gemini/plan-schedule.ts` を作成する。
`EXTRACTION_SCHEMA`（P2）と同じ形で `SCHEDULE_SCHEMA` を定義し、
`weight` の `enum` を 5 段階にする。
`parseScheduleResponse` は P2 の `parseExtractionResponse` と同じく
コードフェンスを剥がしてから `JSON.parse` し、Zod で検証する。

プロンプトには次を必ず含める。

- 今日の日付
- 稼働曜日（日本語の曜日名）・稼働時間帯・1 日の上限分数
- 各タスクの名称・説明・優先度・期限
- 確定済みスケジュールの日時とタスク名（無い場合は「なし」と書く）
- 出力の決まり
  - `starts_at` / `ends_at` は ISO 8601 でタイムゾーン付き
  - 稼働曜日・稼働時間帯の中に収める
  - `weight` は優先度と推定作業工数の**組み合わせ**で 5 段階から選ぶ。
    優先度が高くても数分で終わるものは軽くする
  - `overlap_acceptable` は既存予定と重ねてよいかを重さから判断する
  - `reason` は日本語で、なぜその日時にしたかを書く
  - **重複を避けきれない場合は重ねてよい。** 無理に別日へ動かさない

- [ ] **Step 5: 算出ユースケースの失敗するテストを書く**

`lib/usecases/__tests__/plan-schedule.test.ts`。**Gemini をモックする。**

検証する振る舞い:

1. 完了済みタスクを送らない
2. 未完了タスクが 0 件なら `NO_SCHEDULABLE_TASKS` を返し、AI を呼ばない
3. AI の返した日時が稼働時間外なら `outOfWorkHours` に印を付ける
4. 日時として解釈できない提案は除外する
5. タスク名で照合できない提案は除外する
6. 仮案どうしの重複を検出して返す
7. 確定済みスケジュールとの重複も検出して返す
8. **仮案の段階では `schedules` に保存しない**（リポジトリの書き込みを呼ばない）
9. 想定外の重さは `normal` に丸める

- [ ] **Step 6: ユースケースを実装する**

`lib/usecases/plan-schedule.ts` を作成する。

処理の順序:

1. 未完了タスクを取得（`status !== 'done'`）。0 件なら `NO_SCHEDULABLE_TASKS`
2. 稼働条件を取得（未設定なら `DEFAULT_WORK_SETTINGS`）
3. 確定済みスケジュールを取得
4. AI に送る
5. 返ってきた提案を検証・整形して `ScheduleDraft[]` にする
   - `task_title` で未完了タスクと照合。見つからなければ除外
   - `Date.parse` できなければ除外
   - `ends_at <= starts_at` なら除外
   - `isWorkDay` と `isWithinWorkHours` を満たさなければ `outOfWorkHours: true` を立てる
     （除外はしない。利用者が編集で直せるようにする）
   - `isTaskWeight` を通らなければ `normal`
   - `key` は照合用の一意な文字列（`${taskId}-${index}`）
6. 重複を検出する
   - 仮案どうし: `findOverlaps`
   - 仮案と確定済み: `findOverlaps`
   - **検出はユースケースでは行わず、クライアント側で編集のたびに再計算する**ため、
     ここでは重複情報を返さない。ドラフトの配列と確定済みの配列を返し、
     画面側で `findOverlaps` を使う

> **設計上の注意**: 重複はユーザーが日時を編集するたびに変わる。
> サーバーで一度だけ計算しても編集後は古くなるため、
> **重複の計算はクライアント側で毎回行う**。
> 純粋関数なのでクライアントでもサーバーでも同じ結果になる。

- [ ] **Step 7: Server Action と画面を実装する**

`lib/actions/schedules.ts` に `planScheduleAction` を実装する。
P2 の `extractTasksAction` と同じ形で、Supabase クライアントとリポジトリを組み立てて
ユースケースを呼ぶ。ファイル先頭のページに `export const maxDuration = 120` を付ける。

`app/(app)/projects/[projectId]/schedule/page.tsx` を作成し、
確定済みスケジュールと未完了タスク数を渡して `SchedulePlanner` を描画する。

`components/app/schedule-planner.tsx` に次を実装する。

- 「スケジュールを算出」ボタン
- 送信内容の確認ダイアログ（R-21。**確定済みの予定も送る旨を明記する**）
- 送信内容の説明文（画面に常時表示）
- 算出中の待機表示（最大 2 分）
- 仮案の一覧表示（この段階では編集と確定は Task 4 で追加する）

- [ ] **Step 8: 検証セットを実行する**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

- [ ] **Step 9: 実際に動作を確認する**

1. タスクが 0 件のときに「予定を立てるタスクがありません。」が出る
2. タスクを用意して算出すると、仮案が算出理由と重さ付きで表示される
3. 稼働時間外の提案に印が付く
4. `schedules` テーブルが空のままである（保存していない）
5. `extraction_runs` と同様に、送信内容の確認ダイアログでキャンセルすると実行されない

- [ ] **Step 10: コミットする**

```bash
git add -A
git commit -m "feat(schedule): AIによるスケジュール算出と仮案プレビューを実装

- 稼働条件と確定済み予定を踏まえた算出プロンプトを追加
- 重さを5段階で判定させ、重複の可否も返させる
- AIの返した日時をサーバー側で検証し、稼働時間外には印を付ける
- 仮案の段階では保存しない
- 送信内容の明示と確認ダイアログを実装（R-21）"
```

---

## Task 4: 仮案の編集・重複警告・確定

**ブランチ:** `feature/schedule-editing`

**Files:**
- Create: `components/app/schedule-draft-item.tsx`
- Create: `components/app/overlap-warning-dialog.tsx`
- Create: `components/ui/__tests__/overlap-warning-dialog.test.tsx`
- Modify: `components/app/schedule-planner.tsx`
- Modify: `lib/actions/schedules.ts`（`confirmSchedulesAction` を追加）

**Interfaces:**
- Consumes: `ScheduleDraft`（Task 3）、`findOverlaps` `WEIGHT_LABEL`
  `WEIGHT_OVERLAP_HINT`（Task 1）、`ConfirmDialog`（P2 の UI 改善）
- Produces:
  - `type OverlapPair = { draftKey: string; withLabel: string; kind: 'draft' | 'confirmed' }`
  - Server Action: `confirmSchedulesAction(formData): Promise<Result<number>>`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/schedule-editing
```

- [ ] **Step 2: 重複警告ダイアログの失敗するテストを書く**

`components/ui/__tests__/overlap-warning-dialog.test.tsx`。

```tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { describe, expect, it, vi } from 'vitest'
import { OverlapWarningDialog } from '@/components/app/overlap-warning-dialog'

const pairs = [
  { draftKey: 'k1', draftLabel: '見積もりを提出する', withLabel: '定例会議', kind: 'confirmed' as const },
  { draftKey: 'k2', draftLabel: 'レビューを実施する', withLabel: '見積もりを提出する', kind: 'draft' as const },
]

const base = {
  open: true,
  pairs,
  pending: false,
  onConfirm: () => {},
  onCancel: () => {},
}

describe('OverlapWarningDialog', () => {
  it('open が false のときは描画しない', () => {
    render(<OverlapWarningDialog {...base} open={false} />)
    expect(screen.queryByRole('dialog')).not.toBeInTheDocument()
  })

  it('重複している組み合わせをすべて表示する', () => {
    render(<OverlapWarningDialog {...base} />)
    expect(screen.getByText(/見積もりを提出する/)).toBeInTheDocument()
    expect(screen.getByText(/定例会議/)).toBeInTheDocument()
    expect(screen.getByText(/レビューを実施する/)).toBeInTheDocument()
  })

  it('重複件数を見出しに出す', () => {
    render(<OverlapWarningDialog {...base} />)
    expect(screen.getByRole('dialog').textContent).toContain('2')
  })

  it('確定済みとの重複と仮案どうしの重複を区別して示す', () => {
    render(<OverlapWarningDialog {...base} />)
    const text = screen.getByRole('dialog').textContent ?? ''
    expect(text).toContain('確定済み')
    expect(text).toContain('仮案')
  })

  it('削除ではないため復元不可の文言は出さない', () => {
    render(<OverlapWarningDialog {...base} />)
    expect(screen.queryByText(/復元はできません/)).not.toBeInTheDocument()
  })

  it('承知して確定すると onConfirm を呼ぶ', async () => {
    const onConfirm = vi.fn()
    render(<OverlapWarningDialog {...base} onConfirm={onConfirm} />)
    await userEvent.click(screen.getByRole('button', { name: '重複を承知で確定する' }))
    expect(onConfirm).toHaveBeenCalledTimes(1)
  })

  it('取り消すと onCancel を呼ぶ', async () => {
    const onCancel = vi.fn()
    render(<OverlapWarningDialog {...base} onCancel={onCancel} />)
    await userEvent.click(screen.getByRole('button', { name: '戻って修正する' }))
    expect(onCancel).toHaveBeenCalledTimes(1)
  })

  it('処理中は両方のボタンを無効にする', () => {
    render(<OverlapWarningDialog {...base} pending />)
    expect(screen.getByRole('button', { name: '重複を承知で確定する' })).toBeDisabled()
    expect(screen.getByRole('button', { name: '戻って修正する' })).toBeDisabled()
  })
})
```

- [ ] **Step 3: テストを実行し、失敗することを確認する**

Run: `npm test`
Expected: FAIL（`@/components/app/overlap-warning-dialog` を解決できない）

- [ ] **Step 4: 重複警告ダイアログを実装する**

`components/app/overlap-warning-dialog.tsx` を作成する。
`ConfirmDialog` と同じ構造（`role="dialog"` / `aria-modal` / Escape で取消 /
初期フォーカスは「戻って修正する」側）にするが、次の点が異なる。

- 見出し: `重複している予定が N 件あります`
- 本文: 重複の組み合わせを箇条書きで列挙する。
  `kind` が `confirmed` なら「確定済みの『〇〇』と重複」、
  `draft` なら「仮案の『〇〇』と重複」と書き分ける
- **注意文言は出さない**（削除ではないため）
- ボタン: 「戻って修正する」（secondary）と「重複を承知で確定する」（primary）

`ConfirmDialog` を汎用化して流用することも検討したが、
列挙表示と文言が十分に異なるため独立した部品にする。

- [ ] **Step 5: 仮案 1 件の表示・編集コンポーネントを実装する**

`components/app/schedule-draft-item.tsx` を作成する。表示する内容:

- チェックボックス（確定対象に含めるか）
- タスク名
- **開始日時と終了日時の入力欄**（`type="datetime-local"`）
- 重さのラベル（`WEIGHT_LABEL`）と重複の目安（`WEIGHT_OVERLAP_HINT`）
- 算出理由
- 稼働時間外の印（`outOfWorkHours` が真のとき「稼働時間外です」を赤字で表示）
- **重複の明示**: 重複している相手を列挙する
  「⚠️ 確定済みの『定例会議』と重複しています」のように書く

`datetime-local` は `YYYY-MM-DDTHH:MM` 形式を扱う。
保持している ISO 8601 との相互変換を行うヘルパーを同ファイル内に置く。
タイムゾーンは稼働条件のものを用いる。

- [ ] **Step 6: 算出画面に編集と確定を組み込む**

`components/app/schedule-planner.tsx` を次のように拡張する。

- 仮案を `useState` で保持し、日時の編集で更新する
- **編集のたびに `findOverlaps` を呼び直して重複を再計算する**
  （仮案どうし、および仮案と確定済みの両方）
- 重複件数を画面上部に表示する（0 件なら出さない）
- 「確定」ボタンを押したときの分岐
  - 選択された仮案に重複が 1 件もなければ、そのまま `confirmSchedulesAction` を呼ぶ
  - 重複があれば `OverlapWarningDialog` を開く。
    「重複を承知で確定する」で `confirmSchedulesAction` を呼ぶ
- 確定後は仮案を閉じ、「N 件の予定を確定しました。」を表示して `router.refresh()`

- [ ] **Step 7: 確定の Server Action を実装する**

`lib/actions/schedules.ts` に追加する。

```typescript
export async function confirmSchedulesAction(formData: FormData): Promise<Result<number>> {
  const projectId = String(formData.get('projectId') ?? '')
  const payload = String(formData.get('drafts') ?? '[]')

  if (!projectId) return err('VALIDATION_ERROR', 'プロジェクトが指定されていません。')

  let drafts: ScheduleDraft[]
  try {
    drafts = JSON.parse(payload) as ScheduleDraft[]
  } catch {
    return err('VALIDATION_ERROR', '確定する予定を解釈できませんでした。')
  }

  if (!Array.isArray(drafts) || drafts.length === 0) {
    return err('VALIDATION_ERROR', '確定する予定を選んでください。')
  }

  const supabase = await createServerSupabaseClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) return err('UNAUTHENTICATED', 'ログインが必要です。')

  // 画面から送られた日時を再検証する。クライアントの値を信用しない
  for (const draft of drafts) {
    const start = Date.parse(draft.startsAt)
    const end = Date.parse(draft.endsAt)
    if (Number.isNaN(start) || Number.isNaN(end) || end <= start) {
      return err('INVALID_SCHEDULE_RANGE', '開始と終了の日時が正しくありません。')
    }
  }

  try {
    const count = await createSupabaseScheduleRepository(supabase).createMany(
      drafts.map((draft) => ({
        projectId,
        taskId: draft.taskId,
        startsAt: new Date(draft.startsAt).toISOString(),
        endsAt: new Date(draft.endsAt).toISOString(),
        reason: draft.reason,
        weight: isTaskWeight(draft.weight) ? draft.weight : 'normal',
        createdBy: user.id,
      })),
    )
    revalidatePath(`/projects/${projectId}/schedule`)
    return ok(count)
  } catch {
    return err('UNKNOWN', '予定を確定できませんでした。')
  }
}
```

> **重要**: 重複していても保存を拒否しない。重複の可否は利用者が
> 警告ダイアログで判断済みである。サーバー側で再度弾くと、
> 利用者の判断を無視することになる。ただし**日時の妥当性は必ず再検証する**。

- [ ] **Step 8: 検証セットを実行する**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

- [ ] **Step 9: 実際に動作を確認する**

1. 仮案の日時を編集できる
2. 意図的に重複させると「〇〇 と重複しています」が該当の予定に出る
3. 重複を解消するように編集すると、表示が消える
4. 重複を残したまま確定すると警告ダイアログが出て、組み合わせが列挙される
5. 「戻って修正する」で保存されない
6. 「重複を承知で確定する」で保存される
7. 重複が無い状態で確定すると、警告なしでそのまま保存される
8. `schedules` に保存された内容を Supabase MCP の `execute_sql` で確認する

- [ ] **Step 10: コミットする**

```bash
git add -A
git commit -m "feat(schedule): 仮案の編集と重複警告つきの確定を実装

- 仮案の開始・終了日時を編集できるようにした
- 編集のたびに重複を再検出し、該当の予定に相手を明示する
- 確定時に重複が残っていれば警告ダイアログで組み合わせを列挙する
- 了承があればそのまま保存し、重複を理由に保存を拒否しない
- サーバー側で日時の妥当性を再検証する"
```

---

## Task 5: 月間カレンダービュー

**ブランチ:** `feature/calendar-view`

**Files:**
- Create: `lib/domain/calendar.ts`, `lib/domain/__tests__/calendar.test.ts`
- Create: `components/app/calendar-month.tsx`
- Modify: `app/(app)/projects/[projectId]/schedule/page.tsx`

**Interfaces:**
- Produces:
  - `type CalendarCell = { date: string; inCurrentMonth: boolean; weekday: number }`
  - `function buildMonthGrid(year: number, month: number, weekStartsOn?: number): CalendarCell[][]`
  - `function shiftMonth(year: number, month: number, delta: number): { year: number; month: number }`
  - `function formatMonthLabel(year: number, month: number): string`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/calendar-view
```

- [ ] **Step 2: 失敗するテストを書く**

`lib/domain/__tests__/calendar.test.ts`。検証する振る舞い:

- 2026 年 9 月のグリッドが 6 週 × 7 日、または 5 週 × 7 日で返る
- 各週はちょうど 7 セル
- 月初（2026-09-01 は火曜）の前に前月の日が並ぶ
- 前月・翌月の日は `inCurrentMonth: false`
- 当月の日はすべて含まれ、`inCurrentMonth: true`
- `shiftMonth(2026, 12, 1)` が `{ year: 2027, month: 1 }` になる
- `shiftMonth(2026, 1, -1)` が `{ year: 2025, month: 12 }` になる
- `formatMonthLabel(2026, 9)` が `2026年9月` を返す
- うるう年の 2 月（2028 年）が 29 日まで含まれる

- [ ] **Step 3: 実装する**

`lib/domain/calendar.ts` を作成する。
週の開始は日曜（`weekStartsOn = 0`）を既定とする。
日付は `YYYY-MM-DD` の文字列で扱い、`Date` の月は 0 始まりである点に注意する。

- [ ] **Step 4: 月間ビューを実装する**

`components/app/calendar-month.tsx` を作成する。

- 7 列のグリッド。曜日の見出しは日本語（日・月・火・水・木・金・土）
- 前月・翌月へ移動するボタンと「今月へ」ボタン
- 各セルに、その日の予定を表示する
  - **確定済み**: 通常の枠線と背景
  - **仮案**: 破線の枠線とアクセント色。「仮」バッジを付ける
  - 重複している予定には ⚠️ を付ける
- 予定が多いセルは高さを固定してセル内でスクロールさせる
- 土日はセルの背景を薄く変える
- 稼働日でない日も薄く表示する（稼働条件に従う）

- [ ] **Step 5: 検証セットと動作確認**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

1. 当月のカレンダーが表示される
2. 前月・翌月へ移動できる
3. 確定済みと仮案が見た目で区別できる
4. 重複している予定に印が付く
5. 月をまたぐ予定でも正しい日に表示される

- [ ] **Step 6: コミットする**

```bash
git add -A
git commit -m "feat(schedule): 月間カレンダービューを実装

- 月間グリッドの組み立てを純粋関数として実装
- 前月・翌月・今月への移動を追加
- 確定済みと仮案を見た目で区別し、重複には印を付ける
- 稼働日でない日を薄く表示する"
```

---

## Task 6: .ics 書き出し

**ブランチ:** `feature/ics-export`

**Files:**
- Create: `lib/domain/ics.ts`, `lib/domain/__tests__/ics.test.ts`
- Create: `app/api/projects/[projectId]/schedule.ics/route.ts`
- Modify: `components/app/schedule-planner.tsx`（書き出しリンクを追加）

**Interfaces:**
- Produces:
  - `function escapeIcsText(value: string): string`
  - `function foldIcsLine(line: string): string`
  - `function toIcsUtc(isoDateTime: string): string`
  - `function buildIcs(events: IcsEvent[], now?: Date): string`
  - `type IcsEvent = { uid: string; startsAt: string; endsAt: string; summary: string; description: string }`

- [ ] **Step 1: ブランチを作成する**

```bash
git checkout main
git pull --ff-only
git checkout -b feature/ics-export
```

- [ ] **Step 2: 失敗するテストを書く**

`lib/domain/__tests__/ics.test.ts`。検証する振る舞い:

**`escapeIcsText`**
- `\` を `\\` にする
- `;` を `\;` にする
- `,` を `\,` にする
- 改行を `\n`（リテラルの 2 文字）にする
- 通常の日本語はそのまま通す

**`toIcsUtc`**
- `2026-09-01T09:00:00+09:00` が `20260901T000000Z` になる
- `2026-09-01T00:00:00Z` が `20260901T000000Z` になる
- 解釈できない文字列で例外を投げない

**`foldIcsLine`**
- 75 オクテット以内の行はそのまま返す
- 75 オクテットを超える行は折り返し、継続行が空白 1 個で始まる
- **マルチバイト文字の途中で折らない**（日本語を含む長い行で文字化けしないこと）

**`buildIcs`**
- `BEGIN:VCALENDAR` で始まり `END:VCALENDAR` で終わる
- `VERSION:2.0` と `PRODID` を含む
- イベント数だけ `BEGIN:VEVENT` がある
- 改行が **CRLF** である
- `SUMMARY` にタスク名が入る
- `DESCRIPTION` に算出理由が入る
- イベントが 0 件でも壊れない

- [ ] **Step 3: 実装する**

`lib/domain/ics.ts` を作成する。

折り返しは**オクテット単位**で数える必要がある。
`TextEncoder` でバイト列にしてから 75 バイト以内に分割し、
`TextDecoder` で戻す際にマルチバイト文字を壊さないよう、
文字単位で足しながらバイト数を数える方式にする。

```typescript
export function foldIcsLine(line: string): string {
  const encoder = new TextEncoder()
  if (encoder.encode(line).length <= 75) return line

  const parts: string[] = []
  let current = ''
  let currentBytes = 0
  // 継続行は先頭に空白 1 個が付くため、2 行目以降は 74 バイトまで
  let limit = 75

  for (const char of line) {
    const size = encoder.encode(char).length
    if (currentBytes + size > limit) {
      parts.push(current)
      current = char
      currentBytes = size
      limit = 74
    } else {
      current += char
      currentBytes += size
    }
  }
  parts.push(current)

  return parts.join('\r\n ')
}
```

- [ ] **Step 4: Route Handler を実装する**

`app/api/projects/[projectId]/schedule.ics/route.ts`:

```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { buildIcs } from '@/lib/domain/ics'
import { createSupabaseScheduleRepository } from '@/lib/repositories/schedules'
import { createServerSupabaseClient } from '@/lib/supabase/server'

export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ projectId: string }> },
) {
  const { projectId } = await params
  const supabase = await createServerSupabaseClient()

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return new NextResponse('ログインが必要です。', { status: 401 })
  }

  // RLS により他人のプロジェクトでは 0 件になる
  const schedules = await createSupabaseScheduleRepository(supabase).listByProject(projectId)

  const ics = buildIcs(
    schedules.map((schedule) => ({
      uid: `${schedule.id}@taskmatrix`,
      startsAt: schedule.startsAt,
      endsAt: schedule.endsAt,
      summary: schedule.taskTitle,
      description: schedule.reason,
    })),
  )

  return new NextResponse(ics, {
    headers: {
      'Content-Type': 'text/calendar; charset=utf-8',
      'Content-Disposition': 'attachment; filename="taskmatrix.ics"',
      'Cache-Control': 'no-store',
    },
  })
}
```

> **注意**: `proxy.ts` の `matcher` は `/api` を除外していない。
> 未認証で `/api/...` を叩いた場合はログイン画面へ転送される。
> Route Handler 側でも `getUser()` を確認しており二重に守られる。

- [ ] **Step 5: 画面に書き出しリンクを追加する**

スケジュール画面に「.ics を書き出す」リンクを置く。
`<a href={`/api/projects/${projectId}/schedule.ics`} download>` とする。
確定済みの予定が 0 件のときはリンクを無効にし、
「確定した予定がありません。」と添える。

- [ ] **Step 6: 検証セットと動作確認**

```bash
npm run lint && npm run typecheck && npm test && npm run build
```

1. 確定済みの予定がある状態で `.ics` を書き出せる
2. ダウンロードしたファイルの中身が `BEGIN:VCALENDAR` で始まる
3. 日本語のタスク名が文字化けしない
4. **実際に Apple カレンダー（macOS）で開ける**
5. 予定が 0 件のときはリンクが無効になっている

- [ ] **Step 7: コミットする**

```bash
git add -A
git commit -m "feat(schedule): .ics 書き出しを実装

- RFC 5545 に沿った .ics を自前で組み立てる（依存追加なし）
- エスケープ・75オクテットでの折り返し・UTC変換・CRLF に対応
- マルチバイト文字の途中で折り返さないようバイト単位で数える
- Content-Disposition が必要なため Route Handler で配信する"
```

---

## 自己レビュー結果

**1. 設計書の網羅性**

| 設計書の項目 | 対応タスク |
|---|---|
| §2 確定した仕様判断 | 全タスク |
| §2 重複についての方針 | Task 1（検出）+ Task 4（明示と警告） |
| §3 データモデル | Task 1 |
| §4 R-21 送信内容 | Task 3（プロンプトと確認ダイアログ） |
| §5 アーキテクチャ | 全タスク。Route Handler は Task 6 |
| §6 全体の流れ | Task 3・4 |
| §6.1 算出フロー | Task 3（1〜7）+ Task 4（8〜11） |
| §6.2 タスクの重さ（5 段階） | Task 1（定義）+ Task 3（判定させる） |
| §6.3 出力スキーマ | Task 3 |
| §6.4 重複検出のロジック | Task 1（純粋関数）+ Task 4（画面で毎回再計算） |
| §7 .ics 書き出し | Task 6 |
| §8 エラー処理 | Task 1（コード追加）+ Task 3・4（送出） |
| §9 テストと検証 | 全タスク |
| §10 ブランチ計画 | 各タスク Step 1 |
| §11 依存パッケージ（追加なし） | 全タスク。新規依存を入れない |
| §12 完了条件 1〜17 | Task 2〜6 の動作確認手順に対応 |

**設計書との差分**: 設計書のブランチ計画は 7 本だったが、本計画では
`feature/schedule-schema` に `work_settings` のテーブル定義も含めたため 6 本になった。
テーブルを 2 回に分けてマイグレーションする理由がなく、
1 つのマイグレーションにまとめる方が適用の手間も少ないと判断した。

**2. プレースホルダ**

「TBD」「後で実装」の類は含まれていない。
Task 4 Step 5・6、Task 5 Step 4 は画面構成を文章で示しているが、
表示する項目・状態の持ち方・見た目の区別・文言を具体的に指定しており、
判断の余地を残していない。

**3. 型の一貫性**

- `Result<T>` / `ok` / `err` — P1 から継承、全タスクで同一
- `TaskWeight` / `WEIGHT_LABEL` / `WEIGHT_OVERLAP_HINT` — Task 1 で定義、Task 3・4・5 で参照
- `TimeRange` / `overlaps` / `findOverlaps` — Task 1 で定義、Task 4 で参照
- `WorkSettings` / `DEFAULT_WORK_SETTINGS` — Task 1 で定義、Task 2・3 で参照
- `ScheduleDraft` — Task 3 で定義、Task 4・5 で参照
- `ScheduleRepository` — Task 3 で定義、Task 4・6 で参照
- `IcsEvent` — Task 6 で定義、同タスク内で使用
- `CalendarCell` — Task 5 で定義、同タスク内で使用

**4. 判断の記録**

重複の計算をサーバーではなくクライアントで行う点は、
編集のたびに結果が変わるためである。純粋関数なのでどちらで実行しても
同じ結果になり、サーバーの往復を減らせる。この根拠を Task 3 Step 6 に明記した。
